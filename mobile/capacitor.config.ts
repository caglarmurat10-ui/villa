import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.safiradestan.villayonetim',
  appName: 'Villa Yönetim',
  webDir: 'dist'
};

export default config;
